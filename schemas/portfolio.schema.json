{
  "$id": "portfolio.v1",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Portfolio Upload Input Schema",
  "description": "Schema for portfolio file upload input",
  "type": "object",
  "properties": {
    "file_path": {
      "type": "string",
      "description": "Path to the portfolio CSV/XLSX file to process"
    },
    "normalize_weights": {
      "type": "boolean",
      "default": true,
      "description": "Whether to normalize weights to sum to 1.0"
    }
  },
  "required": ["file_path"],
  "additionalProperties": false
}
